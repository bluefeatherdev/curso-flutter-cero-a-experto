name: advanced_widgets_app
description: "A sophisticated Flutter application demonstrating advanced state management with Riverpod, dynamic theming with persistent color selection and dark mode toggle, NavigationDrawer with grouped sections, reactive counter example with ConsumerWidget, and comprehensive theme customization using provider-based architecture for production-ready patterns."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.1

dependencies:
  animate_do: ^4.2.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.1.0
  go_router: ^17.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
